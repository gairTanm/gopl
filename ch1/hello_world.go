package main //package main is special as it defines a standalone program, not a library

import (
	"fmt" //the fmt package contains functions for printing formatted output and scanning input
)

func main() { //this is where the execution of the program begins
			fmt.Println("Hello, world?")
}
